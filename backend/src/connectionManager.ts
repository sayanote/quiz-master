import { Server, Socket } from "socket.io";

class ConnectionManager {
    /** 
     This class manages all connections of players.
     * accept new player connections/disconnection
     * accept new messages from player and spectator
     * Broadcast messages to all players/spectators
     * Send message to single player
    */
    constructor(io: Server) {

    }



}